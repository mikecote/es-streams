{
	"name": "es-streams",
	"version": "1.0.0",
	"description": "ElasticSearch streams",
	"main": "src/index.js",
	"repository": "https://github.com/mikecote/es-streams.git",
	"author": "Mike Côté",
	"license": "MIT",
	"private": false,
	"scripts": {
		"lint": "eslint .",
		"docker-up": "docker-compose --file ./test/docker-compose.yml up -d",
		"docker-down": "docker-compose --file ./test/docker-compose.yml down",
		"test": "yarn run lint && yarn run mocha",
		"mocha": "mocha 'test/**/test.*.js' --exit",
		"coverage": "nyc --reporter=lcov --reporter=text-lcov yarn run mocha"
	},
	"dependencies": {
		"lodash": "^4.17.10"
	},
	"devDependencies": {
		"assert": "^1.4.1",
		"elasticsearch": "^15.0.0",
		"eslint": "^4.19.1",
		"eslint-config-airbnb-base": "^13.0.0",
		"eslint-plugin-import": "^2.13.0",
		"mocha": "^5.2.0",
		"nyc": "^12.0.2",
		"uuid": "^3.3.2"
	}
}
